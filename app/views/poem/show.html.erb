<h1>Show</h1>
<div>
  <%= @poem.title %>
</div>
<div>
  <%= @poem.description %>
</div>
<%= link_to "ポエムを編集する", edit_poem_path(@poem) if @is_mine%>
<%= link_to "indexに戻る", poem_index_path if @is_mine%>
<%= link_to "homeに戻る", home_index_path unless @is_mine%>
